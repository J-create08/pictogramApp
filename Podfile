# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PictogramApp' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PictogramApp
  pod 'Firebase/Auth'
  pod 'Firebase/Storage'
  pod 'Firebase/Firestore'
  pod 'URLImage'

  target 'PictogramAppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PictogramAppUITests' do
    # Pods for testing
  end

end
